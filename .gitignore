# Python
__pycache__/
*.pyc
*.pyo
*.pkl
*.egg-info/

# Virtual environments
.venv/
venv/
secretary/.venv/

# Environment / secrets
.env
.env.local
.env.production
.env.*.local
skills/**/.env
*.log
**/kiwoom_token.json
.last_daily_alert_sent
secretary/.env

# PEM, key, certificate files
*.pem
*.key
*.p12
*.pfx
service-account*.json
*credentials*.json

# Runtime data (AI output, market snapshots)
brain/
stocks/today_strategy.json

# OS
.DS_Store
node_modules/

# Backups
*.bak
*.v1.*
*.backup

# 실수로 생성된 pip 아티팩트 / 파일명 오류
=*

# openclaw 시스템 파일 (로컬 전용)
TOOLS.md
.telegram_info_buffer.json

# 백테스트 결과 (용량 큼)
backtest/results/

# Excluded directories
archive/
logs/

# gog binary (Google Sheets CLI, 22 MB — 로컬 전용)
gog-docker
gog

# 팀 프로젝트 — 별도 레포
urban-immune-system/

# 런타임 상태 파일
.openclaw/

# Docker secrets / 런타임 환경 파일
.docker-secrets/
.env.runtime
.env.secure

# SQLite / DB 파일 (secretary/agency_memory.db 등)
*.db
*.sqlite3

# ML 학습 아티팩트
catboost_info/

# 감사 백업 (로컬 전용)
brain.bak.*/
